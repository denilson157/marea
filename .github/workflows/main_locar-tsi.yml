name: Build and deploy Node.js app - locar-tsi

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [14.x]


    steps:
      - uses: actions/checkout@v2

      - name: Set up Node.js version
        uses: actions/setup-node@v1
        with:
          node-version: '16.x'

      - name: Install dependencies 📦
        run: |
           npm ci

      - name: Build the app 🏗
        run: |        
          npm run build    
      - name: Start the app 📤
        run: |
          npm start &
          npx wait-on --timeout 60000
